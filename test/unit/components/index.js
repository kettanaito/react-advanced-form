require('../../jsdom.config');

describe('Components', () => {
  /* createField */
  require('./createField.spec');

  /* connectField */
  require('./connectField.spec');

  /* Form */
  require('./Form.spec');

  /* Field */
  require('./Field.spec');

  /* Field.Group */
  require('./Field.Group.spec');
});
