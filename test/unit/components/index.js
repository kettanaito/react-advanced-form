require('../../jsdom.config');

describe('Components', function () {
  /* FormProvider */
  require('./FormProvider.spec');

  /* Form */
  require('./Form.spec');

  /* Field */
  require('./Field.spec');

  /* Field.Group */
  require('./Field.Group.spec');
});
