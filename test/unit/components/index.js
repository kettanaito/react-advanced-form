require('../../jsdom.config');

describe('Components', () => {
  /* connectField */
  require('./connectField.spec');

  /* Form */
  require('./Form.spec');

  /* Field */
  require('./Field.spec');

  /* Field.Group */
  require('./Field.Group.spec');
});
