require('../../jsdom.config');

describe('Components', () => {
  /* connectField */
  require('./connectField.spec');

  /* Form */
  require('./Form.spec');

  /* Field.Group */
  require('./Field.Group.spec');
});
