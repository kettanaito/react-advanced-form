require('../../jsdom.config');

describe('Components', () => {
  /* createField */
  require('./createField.spec');

  /* FormProvider */
  require('./FormProvider.spec');

  /* Form */
  require('./Form.spec');

  /* Field */
  require('./Field.spec');

  /* Field.Group */
  require('./Field.Group.spec');
});
